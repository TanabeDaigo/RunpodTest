{
  "name": "metroai-pod-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "RunPod Pod HTTP gateway (Node.js, MetroJS-compatible envelope)",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
